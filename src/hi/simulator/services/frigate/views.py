"""Top-level views for the Frigate simulator module.

Empty for scaffolding — the simulator's services-tab UI is rendered by
the parent ``hi.simulator.services`` module via the
``service_simulator_manager`` tab dispatch, and the Frigate-shaped
HTTP API lives under ``api/`` (see ``urls.py``).
"""
